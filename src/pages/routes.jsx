import { BrowserRouter, Routes, Route } from "react-router-dom";

const AppRoutes = () => {
    return (
        <BrowserRouter>
            <Routes>
                {/* <Route path="" element={} /> */}
            </Routes>
        </BrowserRouter>
    )
}